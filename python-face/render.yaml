{
  "name": "smart-attendance-face-api",
  "runtime": "python",
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "gunicorn face_api:app -b 0.0.0.0:$PORT -w 2 --timeout 120",
  "healthCheckPath": "/api/face/health",
  "envVars": {}
}
