# 智能考勤系统 - 本地部署说明

## 快速开始（推荐方式）

### 方式一：前端云端 + 后端本地

#### 第一步：下载人脸识别模型文件

1. 从 GitHub 项目下载 `python-face/models` 文件夹：
   - 下载地址：https://github.com/wangwu1-BEEP/kaoqinxtong/tree/main/python-face/models
   - 需要下载以下3个文件：
     - `haarcascade_frontalface_default.xml`
     - `res10_300x300_ssd_iter_140000.caffemodel`
     - `deploy.prototxt`

#### 第二步：运行后端服务

1. 安装 Python 3.8+（如果没有）
2. 进入 `python-face` 目录：
   ```bash
   cd python-face
   ```

3. 安装依赖：
   ```bash
   pip install flask flask-cors opencv-contrib-python numpy scipy gunicorn
   ```

4. 确保模型文件放在正确位置：
   - `models/haarcascade_frontalface_default.xml`
   - `models/res10_300x300_ssd_iter_140000.caffemodel`
   - `models/deploy.prototxt`

5. 运行后端：
   ```bash
   python face_api.py
   ```

6. 看到以下信息表示成功：
   ```
   * Running on http://127.0.0.1:5000
   ```

#### 第三步：访问前端

打开浏览器访问：**https://www.kaoqin-wtj.shop/**

> 注意：前端已配置调用云端API（Cloudflare Tunnel），如果本地后端没有启动，人脸检测功能将不可用。

---

### 方式二：完整本地运行（开发测试用）

如果需要在本机完整测试前后端：

#### 第一步：修改前端API地址

编辑 `src/components/attendance/FaceRecognition.tsx`，将所有：
```
https://applies-citations-cgi-trio.trycloudflare.com
```
改为：
```
http://127.0.0.1:5000
```

#### 第二步：运行前端

```bash
npm install
npm run dev
```

访问 http://localhost:3000

#### 第三步：运行后端

```bash
cd python-face
pip install -r requirements.txt
python face_api.py
```

---

## 常见问题

### Q1: 提示 "ModuleNotFoundError: No module named 'cv2'"
**解决**：重新安装 opencv
```bash
pip uninstall opencv-python opencv-contrib-python
pip install opencv-contrib-python
```

### Q2: 提示 "DNN模型文件不存在"
**解决**：确保 `models` 文件夹中包含完整的3个模型文件

### Q3: 摄像头无法访问
**解决**：
1. 确保使用 Chrome/Edge 浏览器
2. 允许浏览器访问摄像头
3. 使用 HTTPS 访问（本地开发时需要）

### Q4: CORS 跨域错误
**解决**：确保 Flask 后端已启动，CORS 配置已生效

---

## 文件结构

```
kaoqinxtong/
├── python-face/           # Flask 后端
│   ├── face_api.py        # 主程序
│   ├── models/             # 人脸识别模型
│   │   ├── haarcascade_frontalface_default.xml
│   │   ├── res10_300x300_ssd_iter_140000.caffemodel
│   │   └── deploy.prototxt
│   ├── face_data/         # 人脸数据存储
│   │   ├── models/        # 用户训练模型
│   │   └── meta.json     # 用户信息
│   └── requirements.txt
│
└── src/                   # Next.js 前端
    └── components/
        └── attendance/
            └── FaceRecognition.tsx  # 人脸识别组件
```

---

## 技术说明

- **前端**：Next.js + React，部署在 Vercel
- **后端**：Flask + OpenCV（LBPH + DNN 人脸识别）
- **API调用**：前端通过 HTTPS 调用后端 API
- **CORS**：后端已配置允许跨域访问

---

如有其他问题，请检查控制台日志或联系开发者。
